name: OTC Test - Release
on:
  push:
    branches:
      - 'main'
    tags:
      - "*"
jobs:
  test:
    name: OTC test - Release
    environment: dev
    runs-on: [Linux]
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - run: export